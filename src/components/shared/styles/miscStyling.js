import { css } from 'styled-components';

export const labelStyle = css`
  font-weight: 300;
  font-size: 2rem;
`;

export const smallIconStyle = css`
  .smallIcon > svg {
    max-width: 2.5rem;
    width: 2.5rem;
  }
`;
